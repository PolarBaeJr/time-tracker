`task-011` is complete in the shared workspace. I added the realtime migration at [20240101000008_realtime_config.sql](/Users/matthewcheng/Projects/Time%20tracker/worktracker/supabase/migrations/20240101000008_realtime_config.sql), the typed subscription helper at [realtime.ts](/Users/matthewcheng/Projects/Time%20tracker/worktracker/src/lib/realtime.ts), and exported it from [index.ts](/Users/matthewcheng/Projects/Time%20tracker/worktracker/src/lib/index.ts). The helper validates `userId` and incoming payloads, normalizes `old_record`/`new_record`, exposes connection status callbacks, and retries with backoff. The migration also sets `REPLICA IDENTITY FULL` so `user_id`-filtered UPDATE/DELETE events carry usable old-row data.

Verification: `npm run typecheck` passed, and I ran a targeted compiled smoke test covering a valid insert payload and an invalid update/error path. `npm test` is not available in this repo, and the coordinator `run_tests` hook is currently using the parent directory instead of `/worktracker`. I could not create the required git commit because sandbox writes to `.git/index.lock` are blocked, so the changes are present but uncommitted. I also received the orchestrator `wind_down` signal and have paused.