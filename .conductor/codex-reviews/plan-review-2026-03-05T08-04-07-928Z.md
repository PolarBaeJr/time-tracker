# Codex Review: plan-review

**Verdict:** MAJOR_CONCERNS
**Reviewed:** /Users/matthewcheng/Projects/Time tracker/.conductor/plan-v1.md
**Timestamp:** 2026-03-05T08:04:07.928Z

## Raw Output

```json
{
  "review_performed": true,
  "verdict": "MAJOR_CONCERNS",
  "issues": [
    {
      "description": "The monthly goals uniqueness design is incorrect: `UNIQUE (user_id, month, category_id)` does not prevent duplicate overall goals when `category_id` is `NULL` (PostgreSQL allows multiple NULLs in unique constraints). You need a partial unique index for overall goals (`category_id IS NULL`) plus a separate unique rule for per-category goals.",
      "severity": "major"
    },
    {
      "description": "The plan requires foreign keys to `CASCADE` on delete for all relations, which conflicts with the category-management UX (reassign vs delete entries) and can cause unintended historical data loss if deleting a category cascades into `time_entries`.",
      "severity": "major"
    },
    {
      "description": "Offline sync logic is inconsistent with the timer trust model: the queue includes `start_timer`/`stop_timer` actions, but the security model enforces server-derived timestamps (`started_at = now()`), so replaying after reconnect can produce incorrect durations for offline sessions.",
      "severity": "major"
    },
    {
      "description": "Deployment health checks are incomplete: `docker-compose.prod.yml` checks `curl localhost:80/health`, but nginx configuration only defines SPA routing (`/`) and no `/health` endpoint, so production containers may remain unhealthy.",
      "severity": "major"
    },
    {
      "description": "Security-definer hardening is inconsistent: `handle_new_user()` is declared `SECURITY DEFINER` but does not specify `SET search_path`, despite the plan’s own requirement that security-definer functions set search_path explicitly.",
      "severity": "major"
    },
    {
      "description": "OAuth setup instructions reference enabling the deprecated Google+ API, which is outdated and can block implementers from completing Google OAuth configuration correctly.",
      "severity": "major"
    },
    {
      "description": "Task sequencing is inconsistent: Navigation acceptance criteria include OAuth deep-link success, but navigation depends only on auth hooks and not on the deep-link implementation task, creating dependency/test-order ambiguity.",
      "severity": "minor"
    },
    {
      "description": "Coordinator context is currently non-traceable: `get_tasks`, `get_contracts`, `get_decisions`, and `read_updates` all returned empty arrays, so there is no auditable coordination artifact trail for plan changes.",
      "severity": "minor"
    }
  ],
  "summary": "The plan is much stronger and addresses the previously raised schema/type issues, but it still has several major correctness and operability gaps that should be fixed before approval."
}
```

